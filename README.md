# Sentiment Analysis
WORK IN PROGRESS
  
## Development
- Clone the repo

``` python
# Create a virtual environment 
$ pip install virtualenv
$ python3 -m venv venv
$ . venv/bin/activate
$ pip install -r requirements.txt

```

